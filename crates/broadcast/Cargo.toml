[package]
name = "jamstream-broadcast"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

[dependencies]
tiny-skia.workspace = true
ab_glyph.workspace = true
image.workspace = true
thiserror.workspace = true
# The avatar byte cap only: this crate decodes untrusted pictures on the
# public VM and the size it refuses at has to be the one the control plane
# will carry, so `avatar::MAX_BYTES` reads the constant rather than retyping
# it (#232, #387). Every consumer of this crate already links protocol, so
# the edge costs no bytes in any shipped binary.
jamstream-protocol.workspace = true
